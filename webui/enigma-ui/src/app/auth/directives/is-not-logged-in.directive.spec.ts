import { IsNotLoggedInDirective } from './is-not-logged-in.directive';

describe('IsNotLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNotLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
